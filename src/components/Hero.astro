---
const {title, copy} = Astro.props;
---
<div class="flex flex-col justify-center mx-auto p-4 min-h-120 text-center container">
    <h1 class="font-extrabold text-zinc-50 text-2xl">{title}</h1>
    <p class="">{copy}</p>
</div>